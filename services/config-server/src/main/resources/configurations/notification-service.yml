spring:
  data:
    mongodb:
      uri: mongodb+srv://ofektom:Of3ktom!@cluster0.hbrwv.mongodb.net/customer?retryWrites=true&w=majority

      #      username: ofektom
      #      password: Of3ktom!
      #      host: localhost
      #      port: 27017
      #      database: customer
      #      authentication-database: admin
  kafka:
    producer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup, orderGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringSerializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.trusted.packages: '*'
        spring.json.type.mapping: orderConfirmation:com.ofektom.ecommerce.kafka.OrderConfirmation,paymentConfirmation:com.ofektom.ecommerce.notification.PaymentNotificationRequest

  mail:
    host: localhost
    port: 1025
    username: ofektom
    password: ofektom
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

server:
  port: 8040